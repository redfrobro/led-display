#!/bin/bash
# Start LED Matrix daemon with web server
# Delegates to led-demos script which handles virtual environment detection

SCRIPT_DIR="$(cd -P "$(dirname "$0")" && pwd)"
exec "$SCRIPT_DIR/led-demos" --daemon --webserver "$@"
